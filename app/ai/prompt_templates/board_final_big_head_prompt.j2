Create a Final Big Head clue, category, and answer.

User preferences to consider: {{ user_input }}
Take these preferences into account when generating the Final Big Head.

The Final Big Head should be challenging but solvable.

Return the result as a JSON object with the following structure:
{
    "final_big_head": {
        "category": "Category Name",
        "clue": "Final Big Head clue text",
        "answer": "Correct response"
    }
}